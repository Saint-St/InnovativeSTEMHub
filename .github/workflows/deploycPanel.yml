name: Deploy Static Site to cPanel via ftploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up Node.js (required for ftploy)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # Install ftploy
      - name: Install ftploy
        run: npm install -g ftploy

      # Deploy static site via ftploy with retry
      - name: Deploy to server via FTP
        run: |
          ftploy \
            --username="innovative6@innovativestemhub.com" \
            --password="${{ secrets.FTP_PASSWORD }}" \
            --host="ftp.innovativestemhub.com" \
            --port=21 \
            --localRoot="./" \
            --remoteRoot="/public_html/" \
            --exclude=".git,.github,README.md" \
            --timeout=300000  # 5-minute timeout in milliseconds
        shell: /usr/bin/bash -e {0}
        env:
          FTPLOY_TIMEOUT: 300000  # 5-minute timeout
          FTPLOY_RETRIES: 3       # Retry 3 times
        continue-on-error: true  # Allow analysis of partial success

      # Optional: Debug output if failure occurs
      - name: Debug on Failure
        if: failure()
        run: ls -la